# Add project specific ProGuard rules here.
# The app is a single local WebView (no reflection-heavy libraries), so the
# default Android/Kotlin rules are sufficient — nothing custom is needed.

# Uncomment this to preserve line numbers for debugging stack traces from
# release builds if you ever enable minifyEnabled.
#-keepattributes SourceFile,LineNumberTable
#-renamesourcefileattribute SourceFile
